{
  "name": "@more-nopo/nopo-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./build/index.cjs",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=build/index.cjs --loader:.md=text",
    "test": "bun run build && vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
